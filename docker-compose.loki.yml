services:
  loki:
    image: grafana/loki:3.0.0
    command: -config.file=/etc/loki/config.yml
    ports:
      - "3100:3100"
    volumes:
      - ./ops/loki/loki-config.yml:/etc/loki/config.yml:ro
      - loki-data:/loki

  promtail:
    image: grafana/promtail:3.0.0
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki
    volumes:
      - ./ops/loki/promtail-config.yml:/etc/promtail/config.yml:ro
      - ./data/logs:/var/log/clanker/default:ro
      - /Users/jamesvolpe/web/clanky-2/data/logs:/var/log/clanker/clunky:ro
      - promtail-data:/tmp

  grafana:
    image: grafana/grafana:11.0.0
    depends_on:
      - loki
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_AUTH_ANONYMOUS_ENABLED: "false"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./ops/loki/grafana/provisioning:/etc/grafana/provisioning:ro

volumes:
  loki-data:
  promtail-data:
  grafana-data:
