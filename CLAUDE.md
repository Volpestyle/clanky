# Development Rules

Use `AGENTS.md` as the canonical project guidance.

Before working on Clanky agent runtime behavior, Pi `InteractiveMode`, sessions,
model/tool wiring, or harness integration, inspect
`/Users/jamesvolpe/dev/external/earendil-works/pi` and prefer the published
`@earendil-works/pi-*` APIs and local Pi patterns over guesses.
